import { SortByAgePipe } from './sort-by-age.pipe';

describe('SortByAgePipe', () => {
  it('create an instance', () => {
    const pipe = new SortByAgePipe();
    expect(pipe).toBeTruthy();
  });
});
