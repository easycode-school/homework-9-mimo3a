import { SortByAgeLinePipe } from './sort-by-age-line.pipe';

describe('SortByAgeLinePipe', () => {
  it('create an instance', () => {
    const pipe = new SortByAgeLinePipe();
    expect(pipe).toBeTruthy();
  });
});
