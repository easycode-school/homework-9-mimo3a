import { MultiplyTenPipe } from './multiply-ten.pipe';

describe('MultiplyTenPipe', () => {
  it('create an instance', () => {
    const pipe = new MultiplyTenPipe();
    expect(pipe).toBeTruthy();
  });
});
